
/**
 * This is the standalone version of app.js. It only works if you have a single module.
 * XXXjoe Nevermind, I've eliminated the need for this entirely.
 */
/*
window.define = function(name, fn) {
    var module = {id: name, exports: {}};
    function require(name) {}

    require.ready = function(cb) {
        cb();
    }

    var params = [require, module.exports, module];
    fn({ready: function(cb) { cb()), {}, {});
}
*/
